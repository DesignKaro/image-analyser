{
  "name": "image-to-prompt-extension",
  "version": "2.0.0",
  "private": true,
  "description": "Chrome extension for Image to Prompt",
  "scripts": {
    "build": "node build.js",
    "package": "node build.js && node package-zip.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "clean-css": "^5.3.3",
    "esbuild": "^0.24.0"
  }
}
